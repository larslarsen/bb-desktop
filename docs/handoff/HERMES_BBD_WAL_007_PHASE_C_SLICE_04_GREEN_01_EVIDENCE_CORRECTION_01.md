# Hermes Handoff — BBD-WAL-007 Slice 4 Green 01 Evidence Correction 01

You are **Jr Dev — Hermes**. Documentation/Git only.

Protected governance parent: the commit containing this handoff

Read `AGENTS.md`, Green 01 Evidence, Green 01 Evidence Review 01, and `CURRENT_TASK.md`.
Require `HEAD == origin/master ==` the protected parent and a clean index/worktree.

Edit only:

- `docs/testing/BBD-WAL-007-SLICE-04-GREEN-01.md`: 78 lines,
  `386dd63c84b3edf73522b24861dc881c892c26d3d180502dbc290bf936abf3f6`;
- `docs/handoff/CURRENT_TASK.md`: update only leading state/actors/active handoff and
  append the correction result; starting identity is the version in the protected
  parent.

Preserve every valid result and identity. Correct the filesystem attribution, add all
four transcript deviations exactly as the review states, distinguish diagnostic/
reviewer proof from exact-protocol evidence, and replace the staged-scope claim with
the reviewer-verified exact ten-path commit scope. Do not claim the run was wholly
protocol compliant. Set task state to `PHASE C SLICE 4 GREEN 01 EVIDENCE CORRECTED —
REVIEW REQUIRED`, with no authorized source or integration actor.

Do not edit or execute source/tests, Cargo, Node, binaries, formatter, compiler, Clippy,
network, another actor, Slice 5, broader/final acceptance, or the real local-Monero
gate. Run `git diff --check`, stage exactly the two documentation paths, inspect their
complete staged diff and name list, commit exactly:

```text
docs: correct WAL-007 slice 4 green evidence
```

Push exactly with `git push origin master`. After push, use only read-only Git proof of
`HEAD == origin/master`, clean tree, exact message, and two-path scope, then stop.
