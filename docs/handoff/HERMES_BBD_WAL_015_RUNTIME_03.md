# WAL-015 remaining focused checks
Execute ONLY `python3 docs/handoff/HERMES_BBD_WAL_015_RUNTIME_03.py` once in bb-desktop.
Read exact driver, terminal background=true notify_on_complete=true, then process.wait
at most60 seconds per call until actual exit. Source frozen; no edits, formatting,
extra commands, dependencies, Git or user data. Record actual metadata and pins.
RUNTIME02 core8pass1fail reorg pending150M versus120M is under read-only diagnosis.
Run independent transport3, validation2, recovery1+ignoredchild; stop on failure.
Authorized artifacts are the exact runtime03 JSON/evidence named in driver.
