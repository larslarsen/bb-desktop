# WAL-013 bounded native startup readiness correction

Reviewer rejects final-window-security-02 opening falsification: failure occurred before manage. Hermes session20260910_113507_a60981 closed; production restored/rebuilt. UI9, Clippy and normal actual window passed. No security scans ran.

Pinned eframe 0.36.1 post_rendering forcibly maps the first painted frame; glow applies queued Visible(false) after swap_buffers. An instantaneous pre-manage query races this framework transition. Also zero matching windows currently passes vacuously before native initialization. Product policy is that the initialized account window stays hidden until requested; no claim of zero transient blank framework frames. Hidden application UI never renders account/secret content.

Continuing documented Grok-to-Sol escalation: Sol gpt-5.6-sol High, source-only. ONLY test/walletAccountWindowSmoke.node.js starting SHA256 e4bc03f16d5c545c73298c02242011d70af1051a90c9dea29be1305aa9b381bd. Replace the instantaneous initiallyVisible snapshot with bounded native-startup readiness: require exactly ONE matching real window, map_state 0 (unmapped), same window ID continuously observed for at least 250ms before account.manage. Poll every at most 100ms within WINDOW_MS and global deadline, using existing inspectWindows helper and its remaining-time cap. Missing windows or mapped states reset stable time; multiple matching windows must fail. Timeout message: native account window did not initialize hidden. Preserve all manage/open/hide/reopen/EOF/protocol/owned-root cleanup assertions. No production changes, no tests execution, formatters, dependencies, Git, evidence, delegation. Read only this handoff, AGENTS, and relevant test source. Report exact diff, hash, line count and stop. Hermes will execute both normal and unchanged opening-suppression mutation, restoring production in finally.
