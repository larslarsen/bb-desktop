# WAL-013 actual desktop renderer and client-window scope correction

Existing real-host regression is RED before production: green01 and diagnostic01 exit broker1 without window; diagnostic02 exact native error GLXBadFBConfig. Runtime restored. Software probe actor86384/session20260910_124832_aee8ef closed: LIBGL_ALWAYS_SOFTWARE=1 alone still fails; that plus __GLX_VENDOR_LIBRARY_NAME=mesa initializes real client hidden, then Manage maps it and the broker exits0 through cleanup. Host assertion then fails because GNOME exposes BOTH a frame and client with the same title. Thus no accepted host screenshot yet. Use raw records, not Hermes verbal conclusions. Xvfb rendered controls and blank-UI falsification already pass.

Sol gpt-5.6-sol High continued documented native escalation. ONLY four paths below. Author TEST corrections first, then bounded supervisor production; existing actual-host test has already executed red for this graphics bug, so no additional test-red relay is needed. Do not execute tests, formatters, Git, evidence, dependencies or delegation. Read relevant spans; avoid full-file dumps and repeated reviews. All other source, including account_ui/runtime/Cargo, stays unchanged.

Tests first:
- test/fixtures/wallet-broker/x11-window.py: scope every matching/capture/close operation to the owned broker PID via _NET_WM_PID. Require CLI inspect PID (3 argv), close WINDOW_ID PID / capture WINDOW_ID PID (4 argv). Validate positive pid <= signed32 max. Read XGetWindowProperty _NET_WM_PID as XA_CARDINAL, format32, one item/no trailing bytes using Xlib c_ulong representation; always XFree returned storage. Missing/mismatched PID skips the window; retain exact title, dimensions, visibility, XError, image cleanup and bounded capture. This excludes GNOME decoration frames and unrelated same-title windows. No arbitrary host capture.
- test/walletAccountWindowSmoke.node.js: append exact observed owned broker child.pid to ALL helper calls; fail if unavailable, preserve cleanup behavior. Actual spawn expected env on Linux with DISPLAY is fixed DISPLAY/LANG/PATH/XAUTHORITY plus LIBGL_ALWAYS_SOFTWARE='1', __GLX_VENDOR_LIBRARY_NAME='mesa', still omits bogus WAYLAND_DISPLAY. Explicit expected values independent of production helper; input object unchanged. Keep all rendered-pixel/first/reopen/EOF/no-force/owned-store assertions and screenshots.
- test/walletAccountManagement.node.js: update supervisor spawn expectations ONLY for Linux + nonempty sanitized DISPLAY to include those two fixed values. Main factory still copies six display strings and does NOT add GPU variables. Preserve expected own-data copies for original six, all byte/type/accessor bounds and headless/no-DISPLAY output. Test hostile input GPU settings (e.g. '0'/'nvidia'/canaries/getters) cannot override fixed values or be read; Wayland-only/headless receives neither renderer variable. Update DISPLAY boundary table expected output where needed, without weakening original assertions. Keep seven existing groups.

Then production ONLY wallet-broker/supervisor.js cleanEnvironment: after existing six-key own-data filtering, if process.platform === 'linux' and sanitized result.DISPLAY is nonempty, assign fixed LIBGL_ALWAYS_SOFTWARE='1' and __GLX_VENDOR_LIBRARY_NAME='mesa'. Do not add either name to SAFE_ENV; never forward user values. Add concise comment that the small native wallet UI uses Mesa software rendering to avoid host GLX configuration failures. No general Electron/GPU/sandbox change, no native-API behavior change. On other OS or headless retain existing environment exactly. Broker software renderer is deliberate fixed policy, no unsafe Rust env mutation and no package changes.

Hermes green: accountManagement7/startup6/supervisor13/startupSmoke1, rendered Xvfb smoke, actual host XWayland smoke, reviewer visual inspection of screenshots. Preserve accepted prior native blank falsification because widget source unchanged. Verify native binary exact restored hash, unchanged policy baseline and pinned secret scans, then exact integration. No installer work.

Starting hashes:
wallet-broker/supervisor.js 57811beaebdd588f3328925d9b1d1b1d016100bfe1a57000a4ecccba57d60ee0
test/walletAccountManagement.node.js 307480c4620c704af6f5a6f96afda0bf8941d5ee1a7583f6683dc30be0bb62bb
test/walletAccountWindowSmoke.node.js 5661f8945b7cebc331e4a2a1feb290d9ab0b2e77f4979a9adcaf2ac6cb3cdc94
test/fixtures/wallet-broker/x11-window.py 6a049e8dbcd10522d6a7c65f46792afac2c980aa33a413041e118a9d964aa25b
