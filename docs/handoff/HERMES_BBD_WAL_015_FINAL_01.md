# WAL-015 final checksum-record serialization
Execute ONLY `python3 docs/handoff/HERMES_BBD_WAL_015_FINAL_01.py` once in bb-desktop.
Read exactdriver, terminal background=true notify_on_complete=true, process.wait<=60
until EXIT. No production/test/dependency/Git/userdata edits. Only explicitly enumerated
WAL015 generated records may be reformatted by this reviewer-defined operation.
Root verified all64reportedmatches are the public SHA256 of the unchanged secret-hygiene
test file, repeated in hashmaps. The filename keyword caused generic-api-key inference.
Do not change scanner configuration/rules/ignore baseline, drop evidence or hide secrets.
Use standard Unicode escape for the initial s in that exact map key ONLY when paired
with the independently recomputed public file checksum. Python AST and JSON decoded
objects MUST remain exactly equal before/after; record every original/new file hash.
This is lossless serialization of a filename, not redaction or alteration of results.
Only those exact generatedrecordpaths in driver; source/evidence facts remain fixed.
Then default pinnedGitleaksdir mustpass; diffcheck and source/binarypinsverified. Reuse
all reviewed runtime/lint/window/audit/policy results. Future generated records adopt
same equivalent key encoding. No Rust/build/policy reruns. No allowlist or suppression.
