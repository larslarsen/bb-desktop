# Codex Sol Handoff — BBD-WAL-007 Phase-C Slice 5 Correction 02

You are **Principal Dev — Codex Sol at High**, continuing the fill-in because Grok's
usage balance remains exhausted and Correction 01 has two focused blockers.

Repository: `/home/lars/OpenBazaar/bb-desktop`

Protected governance parent: the commit containing this handoff

Read completely before editing: `AGENTS.md`, `TESTING.md`, `tickets/BBD-WAL-007.md`,
Slice-4 Acceptance 01, the original Slice-5 handoff, Slice-5 Source Review 01, the Grok
Stop Review, Correction-01 handoffs, the frozen receiver tests, Correction-01 Source
Review 01, and the complete current source in the two authorized paths.

## Exact scope

Edit only:

- `wallet-broker/src/xmr/account.rs`; and
- `wallet-broker/src/xmr/test_support.rs`.

Their starting identities are the corresponding rows in Correction-01 Source Review
01. Every other source path, every test, manifest, lockfile, document, policy, workflow,
fixture, and repository must remain byte-exact.

Do not run Cargo, rustfmt, tests, Clippy, Node/npm, policy/security commands, builds,
wallet/Monero executables, package managers, network, Git, or GitHub. Do not stage,
commit, push, or maintain evidence. Leave the corrected source unstaged.

## Required correction

Make the live `SystemAccount::view` path implement all ticket states independently.
Preserve input/identity/store proof before side effects. An ordinary failed accepted
local-node probe must produce `NODE_UNAVAILABLE` with no node height/hard-fork, while a
valid probe retains exact syncing/ready height and hard-fork validation. A locked wallet
must produce `LOCKED` without wallet RPC. An absent/dead/unreachable owned child or an
ordinary unavailable wallet RPC must produce `UNAVAILABLE`; an available wallet must be
refreshed and queried through that same authenticated owned child and derive
`WALLET_REFRESHING` or `READY` from validated heights. Do not turn authentication,
protocol, identity, schema, durability, or other integrity failures into ordinary state.
Return only the frozen sanitized view and preserve all balance/capability invariants.

Replace `ReceiverRig::close` / `ReceiverRig::open` raw `PathBuf` transfer with an opaque
public lease type whose path is private and whose value alone carries cleanup ownership
for the exact collision-resistant leaf generated by the rig. `close` moves the guard
into the lease. `open` consumes the lease: on failure the lease cleans its exact leaf;
on success its guard moves into the reopened rig. Dropping a lease without reopening
must clean the leaf. Keep the frozen test call sites source-compatible through type
inference. Cleanup must revalidate the fixed target and exact leaf, reject symlinks, and
never accept an arbitrary caller path or remove a parent/broader directory.

Preserve every other Correction-01 repair and Slice-1–4 invariant. Stop on any need for
a third path, test edit, dependency change, or architecture change. Report exact changed
paths, resulting line counts/SHA-256 values, production state derivation, lease ownership
transfer, and residual concerns.
