# Codex Luna Handoff — BBD-WAL-006 Address Gate Resume 03

You are **Jr Dev — Codex Luna**, using `gpt-5.6-luna`. This durable file is authoritative.

Repository: `/home/lars/OpenBazaar/bb-desktop`

Protected governance parent: the commit containing this handoff

Read completely: `AGENTS.md`, `TESTING.md`, Address Gate 01, Resumes 01/02, all three stop reviews,
all correction handoffs/reviews, Source Review 03, and `CURRENT_TASK.md`.

Restart Address Gate 01 exactly from its protected preconditions and first formatter command.
Reuse no prior result. Resume 02 remains exact except for the new governance parent and corrected
test-support path:

| Path | Lines | SHA-256 |
| --- | ---: | --- |
| `wallet-broker/src/zec/test_support.rs` | 389 | `f7fa31df8f707ead35bba1cd3904c7a4d4b0610bcfe860d710e57a4c12d44ca5` |

The other five source hashes are exactly those in Resume 02/01; total source is 1,854 lines.
Require every original protected input hash, filesystem/disk-path check, diff check, four commands
and exact formatter/Clippy/eight-test/69-5 results, stop rule, evidence item, explicit eight-path
staging, commit message, push, and final proof.

On exact success, evidence must record all prior safe stops and corrections plus this fresh full
rerun: 14 formatter hunks; two `drop_non_drop` deletions and narrow seed-wipe claim; and the 4/4
`AlreadyExists` parallel test-root race with its accepted atomic-directory correction. Do not run
or mutate anything else.
