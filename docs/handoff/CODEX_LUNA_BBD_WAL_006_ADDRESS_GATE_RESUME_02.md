# Codex Luna Handoff — BBD-WAL-006 Address Gate Resume 02

You are **Jr Dev — Codex Luna**, using `gpt-5.6-luna`. This durable file is authoritative.

Repository: `/home/lars/OpenBazaar/bb-desktop`

Protected governance parent: the commit containing this handoff

Read completely: `AGENTS.md`, `TESTING.md`, Address Gate 01, Resume 01, both stop reviews, both
correction handoffs/reviews, Source Review 03, and `CURRENT_TASK.md`.

Restart Address Gate 01 exactly from its protected preconditions and first formatter command.
Reuse no prior result. Resume 01 remains exact except for the new governance parent and the
corrected address path:

| Path | Lines | SHA-256 |
| --- | ---: | --- |
| `wallet-broker/src/zec/address.rs` | 204 | `d9d54e301d188e3d4cb539095c5c01bcb07aaf011fcb1a50129aadc18f24cbfe` |

The other five source hashes are exactly those in Resume 01; total source is 1,839 lines. Require
all original protected input hashes, filesystem/disk-path checks, diff checks, four commands and
their exact formatter/Clippy/eight-test/69-5 results, stop rules, evidence contents, explicit
eight-path staging, commit message, push, and final proofs.

On exact success, evidence must record both prior safe stops: formatter exit 1 followed by the
14-hunk formatting correction and fresh formatter success; then Clippy exit 1 on two
`drop_non_drop` diagnostics followed by their exact deletion and this fresh full rerun. State the
narrow seed-buffer wipe claim from the Clippy stop review. Do not run or mutate anything else.
