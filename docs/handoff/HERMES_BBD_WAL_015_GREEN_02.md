# WAL-015 finish lint, recovery and runnable app verification
Execute ONLY `python3 docs/handoff/HERMES_BBD_WAL_015_GREEN_02.py` once in bb-desktop.
Read exact driver, terminal background=true notify_on_complete=true, process.wait<=60
until actual EXIT. No copying driver, extra commands, dependencies, Git or user data.
GREEN01 actual105tests passed (actor prose115 incorrect); final pins verified (actor
prose claiming otherwise incorrect). All4 mechanical lint fixes reviewed and frozen.
Format only accounts.rs/test_support.rs; rerun impacted core9/manager3, productionClippy.
Reuse105distinct broader test results with reviewed mechanical corrections. Prior
RUNTIME03 transport3/validation2/recovery1 passed. Falsify actual hot-journal recovery
with exact temporary preparation guard; require intended runtime failure, restore bytes
in finally and rerun recovery. Then existing staged broker rebuild with trusted cargo
PATH only, manifest verification, 28distinct JSgroups, Xvfb and actualdesktop owned
empty-profile window proof, exact6 inherited policy failures, pinned Gitleaksdir/diff.
Read-only process log inspection is permitted for this exact owned session if needed.
No user app/profile restart/read/write; no packages/installers. Full commands/limits/
artifacts/pins in exact driver. Build01 was prepared but never launched and superseded.
