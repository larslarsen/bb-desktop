# BBD-WAL-006 Prepare Serde Feature Expected-Red Review 01

Reviewer: Lead Engineer/Reviewer — Codex XHigh

Accepted integrations: `9b3ab712`, corrected evidence `a6042130`

Result: **ACCEPTED — BOUNDED PRODUCTION CORRECTION AUTHORIZED**

The test-first SQLite serde-feature expectation is integrated at its accepted identity. Free Jr
Dev — Hermes (`nous`, `meituan/longcat-2.0:free`) observed the sole exact Node command fail at
68 `ok` / 7 `not ok`; the new seventh failure is the manifest feature-union test, while the six
pre-existing Phase-C policy failures remain exact. The corrected evidence records the real command,
all source identities, five-path worktree inventory, and no-load-error interpretation.

This is a non-vacuous red: the committed policy test now rejects both the old manifest feature
union and the old policy export. Production may add only the pinned SQLite `serde` feature required
by the official PCZT builder, update its matching policy constants/literal, and replace the two
nonexistent PCZT spend-value getters with the public dummy marker/all-signature inspection described
in Prepare Gate Clippy Review 01.

No inventory, resolved-capability, or live-network policy implementation is authorized in this
correction; those remain expected Phase-C partial red. The lockfile remains execution-owned and
must be resolved offline and reviewed before the full gate resumes.
